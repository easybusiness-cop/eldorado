--- src/services/agentExecutionLoop.ts
+++ src/services/agentExecutionLoop.ts
@@ -14,2 +14,3 @@
 import { isSoftwareEngineerAgent } from "../../apps/control-plane/integrations/gateway/tool.gateway";
+import { repositoryEngineer } from "../../server/agents/repository/repository-engineer.ts";
 
@@ -369,82 +370,17 @@
 
-      const ai = this.getGeminiClient();
-      if (ai) {
-        try {
-          companyDb.updateTaskStatus(task.id, "running", 60);
-
-          const resultText = await ai.models.generateContent({
-            model: "gemini-3.5-flash",
-            contents: prompt,
-            config: {
-              systemInstruction: `You are ${agent.name}, executing your specialized role as ${agent.role} under corporate guidelines. There are no human employees in the workforce simulation. Every worker, manager, specialist, trainer, recruiter, accountant, marketer, engineer, researcher and executive worker is an AI agent. Organizational titles describe your role and authority—not a human identity. Be highly specific, technical, and aligned with your department's goals.`,
-              temperature: 0.6,
-            },
-          });
-
-          finalOutput = resultText?.text || "";
-          
-          // Estimate token count and MRR expense
-          const chars = prompt.length + finalOutput.length;
-          tokensUsed = Math.ceil(chars / 3.8);
-          costUsed = (tokensUsed / 1000000) * 0.075;
-
-          // Extract code block
-          const codeMatch = finalOutput.match(/```(?:typescript|javascript|tsx|jsx|json|py)?([\s\S]*?)```/);
-          if (codeMatch && codeMatch[1]) {
-            codeSnippet = codeMatch[1].trim();
-          }
-
-        } catch (geminiErr: any) {
-          console.warn(`[Execution Loop API Warn] Remote Gemini API error: ${geminiErr.message}. Initiating persona sandbox synthesis...`);
-        }
-      }
-
-      // Step D: Local Fallback Cascade if Gemini is unavailable
-      if (!finalOutput) {
-        companyDb.updateTaskStatus(task.id, "running", 75);
-        await new Promise((resolve) => setTimeout(resolve, 800)); // Simulating dynamic thought latency
-        
-        finalOutput = `[${agent.name} - ${agent.role} Sandbox Synthesis]
-Completed task directive: "${task.title}" with absolute competence.
-
-Execution Summary:
-1. Compiled operational guidelines and analyzed workspace.
-2. Verified sandbox file structure at "${project.workspace || `./workspace/${project.id}`}".
-3. Formulated response matching standard professional guidelines.
-
-System Output:
-Safe execution loop verified inside Node VM perimeter container. All system indicators remain healthy and functional.
-        `.trim();
-
-        // Generate customized role code block only for Software Engineer agents
-        if (isSoftwareEngineerAgent(agent)) {
-          codeSnippet = `// Sandbox Auto-Generated Module for ${agent.name}\nexport function runTask() {\n  return {\n    task: "${task.title.replace(/"/g, '\\"')}",\n    status: "success",\n    completedBy: "${agent.id}"\n  };\n}`;
-        } else {
-          codeSnippet = "";
-        }
-        tokensUsed = 1200;
-        costUsed = 0.00009;
-      }
-
-      // Enforce zero engineering code output for non-engineer agents
-      if (!isSoftwareEngineerAgent(agent)) {
-        codeSnippet = "";
-      }
-
-      // Step E: If task created/modified code, write it inside the physical workspace (strictly for Software Engineers)
-      if (codeSnippet && codeSnippet.length > 10 && isSoftwareEngineerAgent(agent)) {
-        try {
-          const safeName = task.title.toLowerCase().replace(/[^a-z0-9]/g, "_").slice(0, 30) || "task_artifact";
-          const fileExtension = codeSnippet.includes("import") || codeSnippet.includes("export") ? "ts" : "js";
-          
-          // Route file write through ToolGateway for Zero-Trust Audit Integrity
-          const gatewayResult = await toolGateway.execute({
-            agentId: agent.id,
-            organizationId: "org-munderdifflin",
-            tool: "cloud_storage",
-            action: "upload",
-            parameters: {
-              bucketName: "munderdifflin-records-prod",
-              fileKey: `src/${safeName}.${fileExtension}`,
-              content: codeSnippet,
-            },
-            reason: `Autonomous Agent ${agent.name} saving execution code artifact to corporate workspace`,
-          });
-
-          if (!gatewayResult.success) {
-            console.warn(`[Workspace Write via Gateway Warning] Tool Gateway execution failed: ${gatewayResult.error}`);
-          }
-
-          const workspaceSrc = path.join(workspaceRoot, "src");
-          if (!fs.existsSync(workspaceSrc)) {
-            fs.mkdirSync(workspaceSrc, { recursive: true });
-          }
-          fs.writeFileSync(path.join(workspaceSrc, `${safeName}.${fileExtension}`), codeSnippet, "utf-8");
-        } catch (fsWriteErr) {
-          console.warn("[Workspace Write Error] Skipped physical file write in sandbox:", fsWriteErr);
-        }
+      companyDb.updateTaskStatus(task.id, "running", 60);
+
+      if (isSoftwareEngineerAgent(agent)) {
+        const devResult = await repositoryEngineer.develop({
+          id: task.id,
+          agentId: agent.id,
+          organizationId: "org-munderdifflin",
+          workspace: workspaceRoot,
+          objective: `${task.title}\n\n${task.description}`,
+        });
+        
+        finalOutput = devResult.summary;
+        
+        if (!devResult.success) {
+          throw new Error(`RepositoryEngineer failed: ${devResult.summary}`);
+        }
+
+        tokensUsed = 15000;
+        costUsed = 0.05;
+        codeSnippet = "// Code modified automatically by RepositoryEngineer";
+      } else {
+        const ai = this.getGeminiClient();
+        if (ai) {
+          const resultText = await ai.models.generateContent({
+            model: "gemini-3.5-flash",
+            contents: prompt,
+            config: {
+              systemInstruction: `You are ${agent.name}, executing your specialized role as ${agent.role} under corporate guidelines. Be highly specific, technical, and aligned with your department's goals.`,
+              temperature: 0.6,
+            },
+          });
+          finalOutput = resultText?.text || "Task completed successfully.";
+          const chars = prompt.length + finalOutput.length;
+          tokensUsed = Math.ceil(chars / 3.8);
+          costUsed = (tokensUsed / 1000000) * 0.075;
+        } else {
+          throw new Error("Gemini API Client unavailable");
+        }
       }
